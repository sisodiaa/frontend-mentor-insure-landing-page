import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://sisodiaa.github.io/",
  repo: "/frontend-mentor-insure-landing-page",
  experimental: {
    assets: true,
  },
});
